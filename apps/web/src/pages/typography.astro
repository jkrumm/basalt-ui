---
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import Layout from '@/layouts/Layout.astro';
---

<Layout
  title="Typography - Basalt UI"
  description="Advanced variable font system: Instrument Sans with width axis (condensed/wide), JetBrains Mono with ligatures. Zero flicker, optimal performance."
>
  <main class="prose container mx-auto max-w-6xl px-4 py-16">
    <!-- Hero Section -->
    <div class="prose mb-16">
      <h1 class="text-display tracking-tight">Typography</h1>
      <p class="text-muted-foreground max-w-2xl">
        Modern variable fonts with width control: <strong>Instrument Sans</strong> for UI text (with <code
          >.font-condensed</code
        > and <code>.font-wide</code> utilities) and <strong>JetBrains Mono</strong> for code (with ligatures).
        Opt-in semantic HTML styling via <code>.prose</code> class.
      </p>
    </div>

    <!-- Semantic HTML Demo Section -->
    <section class="mb-16">
      <h2>Using .prose for Content Areas</h2>
      <Card>
        <CardContent className="pt-6">
          <div class="border-border bg-muted/30 mb-8 rounded-lg border p-6">
            <article>
              <h2>This h2 is styled automatically within .prose</h2>
              <p>
                This paragraph looks great without extra classes. Basalt UI uses the official
                <code>@tailwindcss/typography</code> plugin with the <code>.prose</code> class for opt-in
                semantic styling. Perfect for blog posts, articles, and documentation.
              </p>
              <h3>This is an h3 heading</h3>
              <p>
                Notice how spacing, sizing, and colors are all automatic within <code>.prose</code> containers.
                Instrument Sans provides consistent typography across headings and body text.
              </p>
              <p>
                <a href="#">Links are styled automatically too</a>, with the blue accent color and
                subtle underline that responds to hover.
              </p>
              <ul>
                <li>Lists work automatically</li>
                <li>No classes needed</li>
                <li>Just semantic HTML</li>
              </ul>
              <blockquote>
                Blockquotes are styled with the primary blue border and muted text, creating subtle
                visual hierarchy.
              </blockquote>
              <p>
                Inline <code>code elements</code> get monospace font with background highlighting.
              </p>
            </article>
          </div>
          <div class="text-muted-foreground text-sm">
            👆 All the typography above uses semantic HTML with the <code class="text-xs"
              >.prose</code
            > class on the container.
          </div>
        </CardContent>
      </Card>
    </section>

    <!-- Why .prose Section -->
    <section class="mb-16">
      <h2>Why Opt-In with .prose?</h2>
      <Card>
        <CardContent className="pt-6">
          <div class="prose space-y-4">
            <p>
              Basalt UI uses <strong>opt-in typography</strong> to avoid conflicts with component libraries
              like ShadCN and Tremor. Global semantic HTML defaults would interfere with:
            </p>
            <ul>
              <li>Tremor Table components (expect unstyled <code>&lt;table&gt;</code> elements)</li>
              <li>
                ShadCN Buttons (shouldn't inherit link <code>&lt;a&gt;</code> styles)
              </li>
              <li>Navigation links (don't want automatic blue underlines)</li>
              <li>Component padding/spacing (conflicts with global paragraph margins)</li>
            </ul>
            <p>
              With the <code>.prose</code> class, you choose where semantic HTML styling applies:
            </p>
            <pre
              class="bg-muted overflow-x-auto rounded p-4 font-mono text-xs">{`<!-- Content areas: Use .prose -->
<article >
  <h2>Article Title</h2>
  <p>Automatically styled content...</p>
</article>

<!-- UI components: No .prose -->
<Table>...</Table>
<Button>Click Me</Button>
<nav><a href="#">Link</a></nav>`}</pre>
          </div>
        </CardContent>
      </Card>
    </section>

    <!-- Font Families Section -->
    <section class="mb-16">
      <h2>Font Families</h2>

      <div class="grid gap-6 md:grid-cols-2">
        <Card>
          <CardHeader>
            <CardTitle>Instrument Sans Variable</CardTitle>
          </CardHeader>
          <CardContent>
            <div class="space-y-4">
              <div class="font-heading text-2xl">The quick brown fox jumps over the lazy dog</div>
              <div class="text-muted-foreground text-sm">
                <code class="font-mono">font-heading</code> / <code class="font-mono"
                  >font-body</code
                > • Weights: 400-700 • Width: 85-110%
              </div>
              <div class="text-muted-foreground text-caption">
                Used for all text (headings, body, UI). Variable font with width axis for <code
                  >.font-condensed</code
                > / <code>.font-wide</code> utilities.
              </div>
            </div>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>JetBrains Mono Variable</CardTitle>
          </CardHeader>
          <CardContent>
            <div class="space-y-4">
              <div class="font-mono text-2xl">!== !== {'<='} {'>='} {'=>'}</div>
              <div class="text-muted-foreground text-sm">
                <code class="font-mono">font-mono</code> • Weights: 400-700 • Ligatures enabled
              </div>
              <div class="text-muted-foreground text-caption">
                Used for code blocks, inline code. Code ligatures for operators (!==, {'<='}, {
                  '=>'
                })
              </div>
            </div>
          </CardContent>
        </Card>
      </div>
    </section>

    <!-- Typography Scale Section -->
    <section class="mb-16">
      <h2>Typography Scale</h2>

      <Card>
        <CardContent className="pt-6">
          <div class="space-y-8">
            <!-- Display -->
            <div class="border-border border-b pb-6">
              <div class="text-display tracking-tight">Display Text</div>
              <div class="text-muted-foreground font-mono text-sm">
                text-display • 64px / 77px • 700 weight • -0.5px tracking
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Used for hero sections and large marketing headlines (requires .text-display class)
              </div>
            </div>

            <!-- Hero -->
            <div class="border-border border-b pb-6">
              <div class="text-hero">Hero Text</div>
              <div class="text-muted-foreground font-mono text-sm">
                text-hero • 48px / 58px • 700 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Subhero sections and large page headers (requires .text-hero class)
              </div>
            </div>

            <!-- H1 -->
            <div class="border-border border-b pb-6">
              <h1>Heading 1</h1>
              <div class="text-muted-foreground font-mono text-sm">
                h1 element • 40px / 48px • 700 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Primary page heading (automatic styling, no class needed)
              </div>
            </div>

            <!-- H2 -->
            <div class="border-border border-b pb-6">
              <h2>Heading 2</h2>
              <div class="text-muted-foreground font-mono text-sm">
                h2 element • 32px / 42px • 700 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Section headers (automatic styling, no class needed)
              </div>
            </div>

            <!-- H3 -->
            <div class="border-border border-b pb-6">
              <h3>Heading 3</h3>
              <div class="text-muted-foreground font-mono text-sm">
                h3 element • 24px / 34px • 700 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Subsection headers (automatic styling)
              </div>
            </div>

            <!-- H4 -->
            <div class="border-border border-b pb-6">
              <h4>Heading 4</h4>
              <div class="text-muted-foreground font-mono text-sm">
                h4 element • 20px / 28px • 700 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Card titles (automatic styling)
              </div>
            </div>

            <!-- H5 -->
            <div class="border-border border-b pb-6">
              <h5>Heading 5</h5>
              <div class="text-muted-foreground font-mono text-sm">
                h5 element • 18px / 27px • 700 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Small section headings (automatic styling)
              </div>
            </div>

            <!-- H6 -->
            <div class="border-border border-b pb-6">
              <h6>Heading 6</h6>
              <div class="text-muted-foreground font-mono text-sm">
                h6 element • 16px / 24px • 700 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Tiny section headings (automatic styling)
              </div>
            </div>

            <!-- Body -->
            <div class="border-border border-b pb-6">
              <p>
                Body text: The quick brown fox jumps over the lazy dog. This is the default
                paragraph text style used throughout the design system.
              </p>
              <div class="text-muted-foreground font-mono text-sm">
                p element • 16px / 24px • 400 weight
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Paragraph text and general content (automatic styling, default)
              </div>
            </div>

            <!-- Small -->
            <div class="border-border border-b pb-6">
              <div class="text-sm">
                Small text for metadata, captions, and secondary information
              </div>
              <div class="text-muted-foreground font-mono text-sm">text-sm • 14px / 20px</div>
              <div class="text-caption text-muted-foreground mt-2">
                Metadata and captions (requires .text-sm class)
              </div>
            </div>

            <!-- Caption -->
            <div class="pb-6">
              <div class="text-caption">
                Caption text for legal disclaimers, footnotes, and fine print
              </div>
              <div class="text-muted-foreground text-caption font-mono">
                text-caption • 12px / 16px
              </div>
              <div class="text-caption text-muted-foreground mt-2">
                Legal text and footnotes (requires .text-caption class)
              </div>
            </div>
          </div>
        </CardContent>
      </Card>
    </section>

    <!-- Usage Guidelines Section -->
    <section class="mb-16">
      <h2>Usage Guidelines</h2>

      <div class="grid gap-6 lg:grid-cols-2">
        <!-- Semantic Classes -->
        <Card>
          <CardHeader>
            <CardTitle>Semantic Typography</CardTitle>
          </CardHeader>
          <CardContent>
            <div class="space-y-4">
              <div>
                <p class="mb-2 text-sm font-bold">Recommended for content hierarchy</p>
                <pre
                  class="bg-muted overflow-x-auto rounded p-4 font-mono text-xs">{`<!-- Semantic HTML (automatic styling) -->
<h1>Hero Title</h1>
<h2>Section Header</h2>
<p>Body paragraph text</p>

<!-- Custom sizes when needed -->
<div class="text-display">Extra Large Hero</div>
<span class="text-sm">Metadata or caption</span>

<!-- Markup is cleaner, more semantic -->`}</pre>
              </div>
            </div>
          </CardContent>
        </Card>

        <!-- Utility Classes -->
        <Card>
          <CardHeader>
            <CardTitle>Utility Classes</CardTitle>
          </CardHeader>
          <CardContent>
            <div class="space-y-4">
              <div>
                <p class="mb-2 text-sm font-bold">For flexible custom styling</p>
                <pre
                  class="bg-muted overflow-x-auto rounded p-4 font-mono text-xs">{`<div class="text-2xl font-bold">
  Custom large text
</div>

<div class="text-base tracking-wide uppercase">
  Uppercase label
</div>

<code class="text-sm font-mono">
  Inline code snippet
</code>`}</pre>
              </div>
            </div>
          </CardContent>
        </Card>
      </div>
    </section>

    <!-- Font Weights & Letter Spacing -->
    <section class="mb-16">
      <h2>Weights & Spacing</h2>

      <div class="grid gap-6 md:grid-cols-2">
        <Card>
          <CardHeader>
            <CardTitle>Font Weights</CardTitle>
          </CardHeader>
          <CardContent>
            <div class="space-y-3">
              <div class="font-regular text-xl">font-regular (400) - Body text</div>
              <div class="text-xl font-bold">font-bold (700) - Headings and emphasis</div>
            </div>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>Letter Spacing</CardTitle>
          </CardHeader>
          <CardContent>
            <div class="space-y-3">
              <div class="text-xl tracking-tight">tracking-tight (-0.5px) - Display text</div>
              <div class="text-xl tracking-normal">tracking-normal (0px) - Default</div>
              <div class="text-xl tracking-wide">tracking-wide (0.025em) - Uppercase</div>
            </div>
          </CardContent>
        </Card>
      </div>
    </section>

    <!-- Width Utilities Section -->
    <section>
      <h2>Variable Font Width</h2>

      <Card>
        <CardHeader>
          <CardTitle>Width Utilities</CardTitle>
        </CardHeader>
        <CardContent>
          <div class="space-y-4">
            <div>
              <p class="mb-2 text-sm font-semibold">Standard (100% width):</p>
              <p class="text-base">This is Instrument Sans at normal width. Friendly and modern.</p>
            </div>

            <div>
              <p class="mb-2 text-sm font-semibold">Condensed (85% width) - Data-focused:</p>
              <p class="font-condensed text-base tracking-wider uppercase">
                DASHBOARD ANALYTICS • SYSTEM STATUS • COMPACT UI
              </p>
            </div>

            <div>
              <p class="mb-2 text-sm font-semibold">Wide (110% width) - Spacious, labels:</p>
              <p class="font-wide text-base">Generous spacing for button labels and callouts</p>
            </div>

            <pre
              class="bg-muted mt-4 overflow-x-auto rounded p-4 font-mono text-xs">{`<!-- Condensed for tight, industrial look -->
<div class="font-condensed uppercase tracking-wider text-xs font-semibold">
  System Status
</div>

<!-- Wide for friendly, spacious feel -->
<button class="font-wide text-lg">
  Get Started
</button>

<!-- Code with ligatures -->
<code class="font-mono">!== !== <= >= -> =></code>`}</pre>
          </div>
        </CardContent>
      </Card>
    </section>
  </main>
</Layout>
