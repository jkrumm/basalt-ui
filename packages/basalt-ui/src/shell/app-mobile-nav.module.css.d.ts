/**
 * Typed CSS-module declaration (the S4 CSS-modules path). Named string props — not an index
 * signature — so dot access stays verbatim under the package's strict flags. HAND-MAINTAINED:
 * there is no generator, so a new class in `app-mobile-nav.module.css` must be added here too.
 */
declare const classes: {
  readonly bar: string
  readonly mainSafeArea: string
  readonly tab: string
  readonly tabIcon: string
  readonly tabDot: string
  readonly label: string
  readonly menuDropdown: string
  readonly menuItem: string
  readonly menuItemNested: string
  readonly menuLabel: string
  readonly sheet: string
  readonly sheetTitle: string
  readonly sheetHeader: string
  readonly row: string
  readonly rowNested: string
}
export default classes
