// Tiny fixture consumed by oxlint-preset-sync.test.ts to prove the shipped preset actually
// parses in oxlint (not just structurally, via JSON.parse). Content is intentionally trivial —
// this exercises config-parse success, not lint-rule coverage.
export const fixtureValue = 1
